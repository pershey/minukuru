# Minukuru TestFlight IAP Checklist

## 目的

- `ミヌクル プレミアム` が TestFlight 実機で読み込まれるか
- 購入ボタンから iOS の購入シートが開くか
- 購入後にプレミアム機能が解放されるか

## 前提

- TestFlight に最新ビルドを配布済み
- App Store Connect の課金商品 `com.naoyaochiai.minukuru.premium` がこのビルドに紐づいている
- Paid Apps Agreement / 銀行口座 / 税務情報が完了している

## 確認手順

1. TestFlight から最新ビルドをインストールする
2. アプリを起動する
3. タイトル画面の `プレミアムを見る` を開く
4. 次の 2 点を確認する
   - `プレミアムの状態` が `取得できた` になる
   - `プレミアムを購入する（価格）` ボタンが表示される
5. `プレミアムを購入する` をタップする
6. iOS の購入シートが開くことを確認する
7. 購入完了後、次を確認する
   - `プレミアム機能が使えるようになりました。` と表示される
   - `実施済み問題スキップ` と `現実モード` のトグルが使える
   - 広告が非表示になる
   - 追加問題が反映される

## 見るべき異常パターン

- `商品情報をもう一度確認` のまま変わらない
  - 商品メタデータか App Store Connect 側の設定不足を疑う
- 購入ボタンは出るが、押しても購入シートが出ない
  - アプリ側の購入導線不具合の可能性が高い
- 購入中スピナーが長く続いて結果が出ない
  - App Store sandbox 応答遅延、または購入処理の不具合を疑う

## 今回の修正で変わった点

- 商品取得中
- 権利確認中
- 実際の購入中
- 復元中

これらを別々の状態として扱うようにしたため、商品が取得できているのに `購入中` 扱いのスピナーだけが出て押せない、という状態は起きにくくなっています。

## 補足

- TestFlight の課金テストは sandbox で行われるため、実際の請求は発生しません
- Xcode 用の `MinukuruLocal.storekit` はローカル確認用で、TestFlight では App Store Connect の商品設定が使われます
